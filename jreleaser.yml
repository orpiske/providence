project:
  name: providence-collector
  version: ${project.version}
  description: Providence Collector
  longDescription: Providence Collector is a tool for collecting data from various sources.

release:
  github:
    owner: your-github-username
    name: providence-collector
    tagName: v{{projectVersion}}
    releaseName: Providence Collector {{projectVersion}}
    overwrite: true

distributions:
  providence-collector:
    artifact:
      path: providence-collector/target/providence-collector-{{projectVersion}}-bin.tar.gz
    type: SINGLE_FILE
